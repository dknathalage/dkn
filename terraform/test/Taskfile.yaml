# autogenerated
version: "3"

vars:
  COMPONENT: test

tasks:
  init:dev:
    desc: Initialize Terraform for test in dev environment
    internal: true
    cmds:
      - terraform init -reconfigure -backend-config="prefix=dknathalage/dkn/test/dev"

  plan:dev:
    desc: Plan Terraform changes for test in dev environment
    deps: [init:dev]
    cmds:
      - terraform plan -var-file="tfvars/dev.tfvars"

  apply:dev:
    desc: Apply Terraform changes for test in dev environment
    deps: [init:dev]
    cmds:
      - terraform apply -var-file="tfvars/dev.tfvars" --auto-approve

